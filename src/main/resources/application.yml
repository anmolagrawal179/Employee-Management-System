spring:
  application:
    name: Employee-Management-System
  datasource:
    url: jdbc:mysql://localhost:3306/ems
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
